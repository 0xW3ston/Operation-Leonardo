<div class="details">
    <% const ev = formationsD.description.split("\n"); %>
    <h3><%= formationsD.label %></h3>
    <p>
        <% ev.forEach(e => { %>
            <%= e %> <br>
        <% }); %>
    </p>
    <div>
        <% console.log("pathh ",images)
        images.map(path=> {  %>
            <img src="<%= path.path %>" alt="">

        <% }) %>
    </div>
</div>
